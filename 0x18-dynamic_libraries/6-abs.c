#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 * @abs: to be checked
 * Return: abs
*/
int _abs(int abs)
{
if (abs < 0)
{
return (-abs);
}
else
{
return (abs);
}
}
